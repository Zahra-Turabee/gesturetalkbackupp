name: gesturetalk1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  permission_handler:
  flutter_local_notifications: ^19.1.0
  torch_light: ^1.0.0
  vibration: ^3.1.3
  shared_preferences: ^2.0.15  # Compatible version
  supabase_flutter: ^2.8.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - lib/assets/images/
